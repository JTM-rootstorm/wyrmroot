[package]
name = "wyrmroot-registry-proto"
version = "0.0.0"
edition = "2024"
license = "GPL-3.0-or-later"
publish = false
description = "Allocation-free WYR1 bootstrap registry protocol"

[lib]
path = "src/lib.rs"

[lints.rust]
unsafe_code = "forbid"
unused_crate_dependencies = "deny"
