[package]
name = "wyrmroot-device-proto"
version = "0.0.0"
edition = "2024"
license = "GPL-3.0-or-later"
publish = false
description = "Allocation-free WYR1-C device coordinator model and protocols"

[lib]
path = "src/lib.rs"

[lints.rust]
unsafe_code = "forbid"
unused_crate_dependencies = "deny"
