[package]
name = "wyrmroot-bootfs"
version = "0.0.0"
edition = "2024"
license = "GPL-3.0-only"
publish = false
description = "Wyrmroot bootstrap archive support"

[lib]
path = "src/lib.rs"

[features]
default = []
builder = []
